{
  "name": "pdf-guru-tooling",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky",
    "test": "npm --prefix backend run test && npm --prefix mobile_app run test",
    "lint": "npm --prefix backend run lint && npm --prefix mobile_app run lint",
    "lint:fix": "npm --prefix backend run lint:fix && npm --prefix mobile_app run lint:fix",
    "format": "npm --prefix backend run format && npm --prefix mobile_app run format",
    "format:check": "npm --prefix backend run format:check && npm --prefix mobile_app run format:check",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix mobile_app run typecheck"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1"
  },
  "lint-staged": {
    "backend/src/**/*.{ts,tsx}": [
      "npm --prefix backend run lint -- --fix",
      "npm --prefix backend exec prettier --write"
    ],
    "mobile_app/src/**/*.{ts,tsx}": [
      "npm --prefix mobile_app run lint -- --fix",
      "npm --prefix mobile_app exec prettier --write"
    ],
    "backend/src/**/*.{json,md}": [
      "npm --prefix backend exec prettier --write"
    ],
    "mobile_app/*.{js,json,md}": [
      "npm --prefix mobile_app exec prettier --write"
    ]
  }
}
